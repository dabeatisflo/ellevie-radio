stages:
  - deploy

create-pages:
  stage: deploy
  script:
    - test -f dist/index.html
  pages:
    publish: dist
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  environment:
    name: production
